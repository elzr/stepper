# Session: 20260520-427342

---
*2026-05-20 08:40* <!-- 86e3172c-010d-48ef-8e4b-0dd46a1a954a -->

## Eli:
any idea why all stepper hotkeys aren't working are also all rcmd toggle shortcuts (like ropt+g to toggle figma) aren't working? the stepper hotkeys seem like it could be the hyperkey app misbehaving again but the rcmd shortcuts don't rely on that, so something weird is going on. I'm sure that it's nothing a whole system reload wouldn't fix but I want to understand what broke and fix it preferrably

## Claude:
Confirmed and diagnosed. This isn't Hammerspoon or your config — it's a macOS-level block, and I can prove it.

## Root cause: Secure Input is stuck ON, held by Google Chrome

`ioreg` reports `kCGSSessionSecureInputPID=983`, and **PID 983 is Google Chrome** (running since yesterday 09:31). When an app calls `EnableSecureEventInput` (Chrome does this for password fields) and never releases it, macOS suppresses keyboard delivery to **every CGEventTap system-wide** — the built-in anti-keylogger mechanism. A logout/reboot clears it, which is exactly why you sensed "a full reload would fix it."

## Why both systems died together — a hidden shared dependency

The common factor isn't Hyperkey. It's *"anything built on an event tap"*:

- **Hyperkey** is a CGEventTap (intercepts Caps Lock → synthesizes ⌘⌃⌥⇧). Blocked → Caps Lock stops producing the hyper modifier → hyper-based stepper hotkeys silently fail. Hyperkey is a *victim* here, not the cause.
- **rcmd** is also a CGEventTap (watches right-cmd/ropt + letter). Same block → `ropt+g` and friends dead.

## The fingerprint that proves it

Hammerspoon itself is **healthy** — IPC responds, all **58 hotkeys still registered**, and the console shows a *plain* stepper hotkey firing 2 min ago:

```
08:43:16  trigger: move-to-display:left 'kitty'
```

That's `ctrl+alt+←` (`stepper.lua:1022`). Plain `hs.hotkey.bind` uses Carbon `RegisterEventHotKey`, which secure input does **not** suppress — so those keep working while the event-tap layer (Hyperkey, rcmd) is blacked out. That selective pattern *is* the signature of stuck secure input.

**Quick test for you right now:** `ctrl+alt+arrow` window moves should still work; Caps-Lock-hyper combos and rcmd toggles won't.

## The fix (no full reboot)

Quitting & relaunching **Chrome** releases the lock the instant the process exits — Hyperkey/rcmd taps resume immediately. Chrome restores your tabs and won't re-grab it unless a password field gets focused again.

Want me to quit & relaunch Chrome for you (I'll verify the secure-input PID clears to `0` afterward)? Or if you'd rather not disturb Chrome's session, I can hunt down the specific tab holding the focused login field so you close just that one.


---
*2026-05-20 08:51* <!-- 1fe82df9-ff56-4582-b598-413f8037d098 -->

## Eli:
ohh nice theory, yes, I always forget the name but I was pretty sure Secure Input was at fault here. But is it my Google Chrome the one that's at fault? The Chrome I control and which would be no big deal to restart, or is it the headless chrome of all the CC instances I'm currently running which would be quite disorienting and would be almost equivalent to a full system restart?

## Claude:



---
*2026-05-20 08:52* <!-- d96a9b4c-bdc8-482e-a049-07c887bff1d7 -->

## Eli:
ohh nice theory, yes, I always forget the name but I was pretty sure Secure Input was at fault here. But is it my Google Chrome the one that's at fault? The Chrome I control and which would be no big deal to restart, or is it the headless chrome of all the CC instances I'm currently running which would be quite disorienting and would be almost equivalent to a full system restart? Otoh, reading you more carefully, you even claim to be able to find the specific tab holding the focused login, so it seems to be definitely in my Chrome. Let's try that, it would be mighty impressive of you to find it :)

## Claude:
Got it — here's the verdict.

## The culprit tab

The Secure Input lock is held by **your** Chrome (PID 983, profile **"Eli – main"**), and the focused field lives on exactly one page:

| | |
|---|---|
| **Tab** | Instituto del Volcán que Ronca — MAS Dashboard 2.0 |
| **URL** | `https://simulado.mas2.internal/hospital/ACH/` |
| **Profile** | Eli – main |

That's your MAS2 simulado dashboard — you left a **focused password field** on its login, and Chrome enabled Secure Input for it and never let go (the classic bug: it doesn't release on losing app focus). Your 5 headless MCP Chromes are untouched and were never involved.

## The fix — no Chrome restart, no reboot

Just **make that password field lose focus**, and Chrome will call `DisableSecureEventInput` instantly:

1. Switch to that **MAS Dashboard** tab.
2. Click the address bar (**⌘L**) — or click any non-password spot on the page, or just finish/submit the login, or close the tab.

The moment the field blurs, Hyperkey, rcmd, and your hyper-based stepper hotkeys all come back to life — no restart of anything.

Go ahead and do that, then tell me — I'll re-check `kCGSSessionSecureInputPID` and confirm it dropped to `0` (and that your taps are live again). If for some reason blurring doesn't release it, quitting & relaunching just that Chrome is the fallback.

---

**Why both systems died (the takeaway):** the common thread was never Hyperkey — it's that Hyperkey *and* rcmd are both **CGEventTaps**, and Secure Input blackholes every event tap at once. Plain Carbon `hs.hotkey.bind` combos like `ctrl+alt+arrow` are immune, which is why `move-to-display` kept firing in the console throughout. I saved this as a memory so next time "hotkeys broke" → I check Secure Input first.


---
*2026-05-20 09:02* <!-- e8e75fe1-7c04-4735-8c01-996f43593f7d -->

## Eli:
hmm that's unlikely to have been the right tab... I closed it altogether and still no response... any other candidates or should I try restarting my whole Chrome?

## Claude:
## Verdict: yes, restart Chrome — it's the only fix now

No external process can decrement another process's Secure Input count, and there's no field left to blur, so the orphaned lock lives until **process 983 dies**. Your instinct is right.

Least-disruptive way to keep your session:
- Type **`chrome://restart`** in any Chrome address bar → Chrome relaunches itself and restores your tabs/windows. (Your **DevTools** windows won't come back, but the pages will.)
- Or **⌘Q** and reopen if you have "Continue where you left off."

Then tell me — I'll re-check `kCGSSessionSecureInputPID` and confirm it flips to `0`, with Hyperkey/rcmd/stepper all live again.

---

One **prevention** note for next time, since you hit this on an internal login: when a password field is focused and you want to bail, click elsewhere (**⌘L**) *before* closing the tab. Closing the tab while the field has focus is precisely what orphans the lock — which is the trap we just fell into when closing the MAS tab didn't help. I've updated my memory accordingly so I'll go straight to "restart Chrome" if the all-windows scan ever shows zero secure fields again.


---
*2026-05-20 09:12* <!-- df494f00-7674-419a-8527-6f37d101dcfe -->

## Eli:
nice chrome://restart tip! I didn't know it and I love learning new hacks

your theory was right, restarted chrome and stepper hyperkeys and rcmd now work. I'm elated by this fix, let's add a case-studies/ fodler at root in stepper and make this the first one (we have case studies inside features) but this is such a massive win that it deserves to be at root. I had been plagued by this orphaned Secure Input lock and could only fix it when it happened after I had opened System Preferences (by closing it), and even stumbling into finding that had been untold pain. Now we have a clear fix for what's surely a common cause (Chrome is one of my most used apps), yay!

## Claude:
Done. Created [`case-studies/`](openfolder:///Users/sara/Library/CloudStorage/Dropbox/projects/log/2025/hammerspoon/stepper/case-studies) at the stepper root with the flagship first entry:

**[`2026-05-20-orphaned-secure-input-kills-all-hotkeys.md`](openfile:///Users/sara/Library/CloudStorage/Dropbox/projects/log/2025/hammerspoon/stepper/case-studies/2026-05-20-orphaned-secure-input-kills-all-hotkeys.md)**

It follows your F027/L007 house format — GICV block, the selective-death fingerprint table (Carbon survives / event taps die), the MCP-Chrome AppleScript trap, why closing the tab orphaned the lock, and the `chrome://restart` fix. I wrote it honestly, including **meta-lesson #3** owning my wrong first call (recommending the tab-close before scanning all windows).

Two things baked in for posterity:
- The **generalized** diagnosis — `ioreg ... kCGSSessionSecureInputPID` → find the PID → quit/release that app — works for *any* holder (Chrome today, System Settings on your past episodes, Terminal's Secure Keyboard Entry, 1Password…), so you're no longer stuck stumbling into it.
- A note tying it to your history: previously the only cure you'd found was closing System Settings, with no recourse when Chrome held it.

The fix itself is verified live: `kCGSSessionSecureInputPID` is gone from `ioreg`, and you've confirmed stepper/Hyperkey/rcmd are all back.

The folder + file are new and untracked — say the word and I'll commit them (along with the memory I added under [`secure_input_breaks_hotkeys.md`](openfile:///Users/sara/.claude/projects/-Users-sara-Library-CloudStorage-Dropbox-projects-log-2025-hammerspoon-stepper/memory/secure_input_breaks_hotkeys.md)).


---
*2026-05-20 09:26* <!-- 139a7ca3-0cac-419f-b4ad-364d28a41fc9 -->

## Eli:
<command-message>commit</command-message>
<command-name>/commit</command-name>

## Claude:
Scoped. The only file from **this** conversation is the new `case-studies/` entry. Per your global rule, I'll also include the do-done logs (tracked across all projects). Everything else in the tree is from other sessions or runtime and I'll leave it unstaged.

**Leaving unstaged (not this conversation):**

| File | Source |
|------|--------|
| `data/bear-notes.jsonc` (M) | Bear note config — other session |
| `data/live-toggle-hotkeys.json` (M) | Live-toggle config — other session |
| `features/L009-keymap/keymap.html` (M) | L009 keymap viewer — other session |
| `data/window-layout-{1,2,3,5,manual-5}.json` (??) | Hammerspoon runtime layout auto-saves |
| `features/L005-.../__pycache__/` (??) | Python bytecode cache (likely should be gitignored) |

Staging the case study + the three do-done logs.

